# Production: point at your Laravel backend (must match CORS + Sanctum stateful domains).
# VITE_API_URL=http://localhost:8000

# Dev: leave VITE_API_URL unset — Vite proxies backend routes to VITE_API_PROXY_TARGET.
# VITE_API_PROXY_TARGET=http://127.0.0.1:8000
